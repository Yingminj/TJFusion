# Fast-FoundationStero legacy (camera + depth combined).
# The active build uses requirements.depth.txt (see Dockerfile.depth).
# numpy / pyzmq / pyyaml / opencv-python are provided by tjfusion-gpu-base.
timm
einops
omegaconf
scipy
scikit-image
imageio
open3d
pyrealsense2
